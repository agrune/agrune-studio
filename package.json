{
  "name": "agrune-studio",
  "version": "0.1.0",
  "private": true,
  "description": "Agrune Studio CLI MVP for creating, validating, editing, and printing Agrune manifest JSON files.",
  "type": "module",
  "packageManager": "pnpm@10.23.0",
  "bin": {
    "agrune-studio": "./dist/cli.js"
  },
  "scripts": {
    "build:engine": "pnpm --dir ../agrune run build",
    "build": "pnpm run build:engine && tsc -p tsconfig.json",
    "test": "pnpm run build && node --test \"dist/**/*.test.js\"",
    "start": "node ./dist/cli.js"
  },
  "dependencies": {
    "@agrune/manifest": "link:../agrune/packages/manifest",
    "agrune": "link:../agrune",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
